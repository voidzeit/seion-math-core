# SEION-KGR v26 MAX — claim matrix

One row per claim referenced in `SEION_KGR_MATHEMATICAL_CONTRACT.md`.
Columns mirror `claims/claim_evidence_matrix_v3.csv`: status, the claim
in one sentence, the theorem/proposition it depends on (inherited or
new), implementation/test location (`TODO` where Fase 1–3 has not
produced the file yet — Fase 0 is contract-only), and the limitation
that keeps the status from being stronger. `CLM_KGR_*` IDs are new;
`THM_V3_*` / kernel-paper items are inherited unchanged from their
source registries.

| Claim ID | Status | Claim | Theorem/evidence dependency | Implementation / test | Limitation |
|---|---|---|---|---|---|
| CLM_KGR_001 | DEFINITION | Reciprocal-relation closure reduces head/tail prediction to one query form `(s,p,?)`. | none (definition) | `seion_kgr_reference_fp64.py::reciprocal_closure`, `build_reciprocal_kg`; self-test `reciprocal_closure_structural` | Structural check only (every base triple's mirror is present); does not yet measure whether it fixes v25's head/tail asymmetry (A15) — that requires a trained run, not this oracle |
| CLM_KGR_002 | PROVED | CP ternary law `μ_ℓ(x,a,q)=O_ℓ[(A_ℓx)⊙(B_ℓa)⊙(C_ℓq)]` equals the dense contraction of `K_{ℓ,dijk}=Σ_α O_{ℓ,dα}A_{ℓ,αi}B_{ℓ,αj}C_{ℓ,αk}`. | `docs/definitions/nary_laws.md` (`CPLaw`), same proof pattern | `seion_kgr_reference_fp64.py::CPTernaryLaw.forward` vs `.dense_tensor_explicit_loops`/`.forward_via_dense` (independent nested-loop path); self-test `clm_kgr_002_cp_dense_equivalence` — measured max abs error `8.9e-16` (FP64) | Algebraic identity only; says nothing about approximation quality of a fixed rank `R_ℓ` |
| CLM_KGR_003 | DEFINITION | CP gauge group (per-component scale product 1, plus permutation) leaves the reconstructed tensor and induced scores invariant. | `docs/definitions/nary_laws.md` gauge ops | `seion_kgr_reference_fp64.py::CPTernaryLaw.{gauge_transform,permute_components}` + `seion_kgr/kernels.py::CPTernaryLaw.permute_components_`; `tests/kgr/test_algebra.py` (scale + permutation, both with negative controls proving the checks aren't vacuous), `tests/kgr/test_seion_kgr_package.py` (batched version) | Group is declared, not proved maximal (larger symmetry groups are not ruled out) — both halves (scale, permutation) are now tested |
| CLM_KGR_004 | NUMERICALLY_TESTED (inherited) | Raw CP factors are not comparable across seeds/resolutions even at matched loss. | `academic_submission_package_v2_20260801/sources/numerical_study/main.tex` (3 seeds → pairwise near-orthogonal subspaces; factors don't persist across 3 resolutions) | already executed, cited | Finding is from the finite-tree track's CP experiments, not yet re-run inside a KGR trainer specifically |
| CLM_KGR_005 | PROVED-by-construction | `Π_cyc = (1/a)Σσ^j` is idempotent and self-adjoint. | Frobenius-unitary `σ`; same proof class as `numerical_study`'s verified `UU*` idempotence | `seion_kgr_reference_fp64.py::cyclic_projector`; self-test `clm_kgr_005_cyclic_idempotent` — measured max abs error `2.2e-16` | Idempotence checked; self-adjointness under the Frobenius inner product not yet separately asserted in code (only idempotence and the identity holds regardless of data) |
| CLM_KGR_006 | DEFINITION | Five-input associator `A^(5)` and anchored binary associator `A_{μ,e}^(3)` are distinct objects with distinct domains. | `docs/definitions/associators.md` | `TODO` | — |
| CLM_KGR_007 | OPEN | Reducing associator/FI/closure energy improves KGE link-prediction metrics. | none — flagged explicitly as an unproved causal claim in the source design note | `TODO: ablation KGE-only vs KGE+geom (Gate 11)` | No experiment in this repo isolates this for link prediction; `numerical_study`'s associator-ratio-vs-dimension result is a structural observation, not a link-prediction result |
| CLM_KGR_008 | EXPLORATORY | Filippov-identity energy `L_FI` is a useful regularizer for the ternary law. | none | `TODO` | Even a fully reduced `L_FI` does not establish a 3-Lie algebra without separately verified total antisymmetry (A10) |
| CLM_KGR_009 | PROVED_UNDER_ASSUMPTIONS (inherited) | Closure residual `r_μ=(I-P)μ(P·,…,P·)` and its operator norm `ρ_μ` are well-defined; sample-mean `L_closure` is a training proxy, not a bound on `ρ_μ`. | `docs/definitions/projectors.md` | existing v3 suite; retyped for messages in `seion_kgr_reference_fp64.py::closure_residual`, `MessagePassingLayer.closure_leakage`; self-test `closure_leakage_full_rank` confirms exact zero leakage at `P=I` | Assumes A1/A2 (isometry, orthogonal projector); only the trivial `P=I` case is checked so far, not a genuine rank-reduced leakage measurement (that needs Fase 2/6) |
| CLM_KGR_010 | PROVED_UNDER_ASSUMPTIONS (inherited) | Exact local error decomposition `Δ_v = r_v + Σ_{S≠∅} μ_v(Y^S)` holds for any typed multilinear tree node. | `docs/theorems_v3/exact_subset_expansion.md` (`THM_V3_EXACT_SUBSET_EXPANSION`) | `src/seion_core/research_v3/error_expansion.py`; **conformance-tested** against our concrete `CPTernaryLaw` in `tests/kgr/test_recursive_projection.py::test_exact_subset_expansion_holds_for_cp_ternary_law` (verifies the identity numerically for the actual KGR object, not just the abstract theorem), with a negative control (`test_exact_subset_expansion_fails_for_a_non_multilinear_law`) confirming the check correctly fails for a law that violates the multilinearity hypothesis, and a `k=1` corollary check (`test_k_equals_1_corollary_error_is_entirely_normal`) | Applies to the certified core `C_ℓ` only (A5), not to a full nonlinear layer; the conformance test covers a single node (`k=1` composition depth as far as `CPTernaryLaw` alone goes), not a multi-layer message-passing stack |
| CLM_KGR_011 | PROVED_UNDER_ASSUMPTIONS (inherited) | Root error Pythagorean split `(E_amb)²=(E_proj)²+(E_normal)²`, `E_red=E_proj`. | `docs/theorems_v3/typed_model.md` (`THM_V3_ROOT_ERROR_ORTHOGONALITY`) | `src/seion_core/research_v3/projected_evaluation.py` | Requires A1, A2 |
| CLM_KGR_012 | PROVED_UNDER_ASSUMPTIONS (inherited) | `E_amb ≤ k ρ M^{k-1} L_T`. | `docs/theorems_v3/homogeneous_constants.md` (`THM_V3_HOMOGENEOUS_AMBIENT_K`) | `src/seion_core/research_v3/certificates.py` | Requires uniform `M`, `ρ` (A3, A4); tightness open |
| CLM_KGR_013 | PROVED_UNDER_ASSUMPTIONS (inherited) | `E_proj = E_red ≤ (k-1) ρ M^{k-1} L_T`. | `docs/theorems_v3/homogeneous_constants.md` (`THM_V3_PROJECTED_ROOT_K_MINUS_ONE`) | `src/seion_core/research_v3/certificates.py` + regression test | Same as above; sharpness at fixed `η=ρ/M` explicitly `OPEN` there |
| CLM_KGR_014 | PROVED_UNDER_ASSUMPTIONS (inherited) | Pathwise majorant closed form `B̂_root = Σ_v λ_v ∏_e h_e` is a valid recursion-derived upper bound for a scalar majorant recursion. | `docs/theorems_v3/nodewise_certificates.md` (`THM_V3_NODEWISE_PATH_SUM`) | `src/seion_core/research_v3/certificates.py` | Bounds a cost-style recursion, not necessarily the tightest available certificate (repo reports observed dominance cell-by-cell, proves only minima among valid certificates) |
| CLM_KGR_015 | NUMERICALLY_TESTED (inherited) | Pathwise rank allocation beats `singular_energy` but loses to `uniform` and `local_error_greedy` at equal budget (confirmatory, preregistered). | `applications/adaptive_tensor_network/results/LEVEL1_FINDINGS.md` | already executed (100 measured triples, Level 1) | Confirmatory only for Level 1's synthetic design; Levels 2–3 are exploratory and give mixed/partially-positive results |
| CLM_KGR_016 | NUMERICALLY_TESTED (inherited) | Pathwise majorant is empirically a valid upper bound (`true/majorant ∈[0.35,0.93]`, never `>1`) and correlates strongly with true error (Pearson 0.933). | `applications/adaptive_tensor_network/results/LEVEL1_FINDINGS.md` | already executed | Not a proof; resolves the "orientation inconsistency" flagged in the source note (see contract §I correction) — must stay labeled `empirical_pathwise_majorant` |
| CLM_KGR_017 | DEFINITION | Rank controller uses a feature vector `φ_v` (leakage, pathwise score, singular energy, gradient sensitivity), not pathwise score alone. | direct consequence of rejecting A12 (pathwise-only policy already falsified) | `seion_kgr/rank_controller.py::hybrid_feature_policy` + `POLICIES` registry (uniform/singular_energy/gradient_sensitivity/pathwise/local_error_greedy/hybrid/random); `compare_policies` harness; tests in `tests/kgr/test_seion_kgr_package.py` (`test_hybrid_policy_is_not_identical_to_pathwise_alone`, `test_local_error_greedy_favors_higher_leakage_module`, `test_all_policies_respect_max_rank_and_budget`) | Feature weights are fixed defaults (0.3/0.2/0.25/0.25), not validated per dataset yet — that needs a real Gate 12/13 campaign, not just the unit tests here; Fisher/cost/latency features from the contract's `φ_v` are not yet in `ModuleDiagnostics` |
| CLM_KGR_018 | OPEN | `E_8`'s specific algebraic structure (vs. random scale-matched/permuted/sign-shuffled controls) causes measured predictive gain in a KGR residual branch. | none yet | `TODO: mandatory control battery, Gate 7` | Directly informed by v25: fixed-`E_8` law underperformed bilinear baseline by 25.9% MRR and showed severe head-ranking collapse (`SEION_V25_DESIGN.md`) — no causal attribution has been established even for that weaker claim |
| CLM_KGR_019 | PROVED (elementary) | Margin `> 2ε` in score space implies exact rank-order preservation (Theorem 30.1 form). | elementary triangle inequality, proved directly in the contract §XIV | `seion_kgr_reference_fp64.py::margin_preserves_order`; self-tests `clm_kgr_019_margin_certified_case` (positive case, worst-case perturbation still preserves order) and `clm_kgr_019_negative_control_order_flips_below_margin` (Gate-7-style falsification: order provably flips when the margin condition is not met) | Only as strong as the certified `ε = L_ψ B_state` feeding it (A17) — if `ε` is estimated rather than certified, the resulting "coverage" number is not a certificate; the oracle checks the theorem in isolation, not the full state→score chain (CLM_KGR_020) |
| CLM_KGR_020 | OPEN | A certified `B_state` (from the composed core+Lipschitz-envelope bound) composed with a certified `L_ψ` yields a non-trivial certified-coverage contribution to MRR on a real KG dataset. | contract §XIV, depends on CLM_KGR_012/013 (certified core) and A6/A16 (checked Lipschitz constants) | `TODO` | This is the one genuinely new mathematical claim of SEION-KGR beyond the existing repo; not established anywhere yet — must not be described as inherited |
| CLM_KGR_021 | PROVED_UNDER_ASSUMPTIONS (inherited) | `L^2` ternary kernel operator `T_K` is bounded: `‖T_K(f,g,h)‖₂ ≤ ‖K‖_{L^2(X^4)}‖f‖₂‖g‖₂‖h‖₂`. | `papers/kernel_integrated_laws_v5/main.tex` §XV Proposition (Cauchy–Schwarz + Fubini) | paper only, no repo test cited | Only relevant if/when KGR invokes the continuous kernel extension (not needed for finite discrete KG case, A18) |
| CLM_KGR_022 | PROVED_UNDER_ASSUMPTIONS (inherited) | Composite associator kernel `Φ_K = K_L - K_R` represents `A_K^(5)` exactly; `Φ_K=0` a.e. iff associator vanishes on all test-function products (under density/integrability conditions). | `papers/kernel_integrated_laws_v5/main.tex` §"Kernel of the associator" | paper only | Conditional on declared density/integrability hypotheses, not universal |
| CLM_KGR_023 | PROVED (inherited) | Finite cohomology descent: `Td=dT ⟹ T` induces `T_*: H^p→H^p`; Hodge compatibility `[T,d]=[T,d*]=0 ⟹ [T,Δ]=0`. | `claims/theorem_registry.yaml` (`THM_COHOMOLOGY_DESCENT_FINITE_V1`), `claims/theorem_registry_v2.yaml` (`PROP_FINITE_COHOMOLOGY_DESCENT_V2`) | existing registries | Finite/truncated complexes only; no microlocal or continuum upgrade implied |
| CLM_KGR_024 | OPEN (inherited, explicit) | Spectral dimension `d_s` is well-defined for a KG-derived Markov Laplacian. | `papers/kernel_integrated_laws_v5/main.tex` remark: existence of the `t↓0` heat-trace limit is an additional hypothesis | none | Do not compute `d_s` for a KG operator without checking the limit exists (A19) |
| CLM_KGR_025 | REJECTED (inherited, explicit) | A decreasing sequence of finite-resolution transport defects establishes a continuum/transfer limit. | `papers/kernel_integrated_laws_v5/main.tex` explicit remark | — | Never invoke for ULTRA-style cross-graph transfer claims (A20) without new theory |
| CLM_KGR_026 | DEFINITION | KGE scorer set (reciprocal ComplEx/DistMult/CP/TuckER base experts, CP-ternary scalar `s_seion`, path scorer `s_path`, residual router `s = s_base+γ_r s_path+η_r s_seion`, near-zero init). | motivated by `SEION_V25_DESIGN.md` postmortem (bilinear 0.201 > fixed-E8 0.149 > CP-128 ≈0.079 MRR) | `seion_kgr/scorers.py` (4 base experts), `seion_kgr/kernels.py::SeionicScalarScorer`, `seion_kgr/reasoner.py::PathReasoner`, `seion_kgr/model.py::SeionKGRv26` (router, `sigmoid(gamma_raw)`/`sigmoid(eta_raw)` init at `sigmoid(-4)≈0.018`); self-test in `seion_kgr/train.py::run_self_test` exercises all 4 base experts × path on/off × seion on/off = 16 combinations, all PASS; smoke-run on real WN18RR data for all 4 base experts (`runs/KGR_V26_SMOKE_WN18RR_*`) and on a real WN18RR slice with path+seion+projection jointly enabled (`runs/KGR_V26_SMOKE_PATH_SEION`, `runs/KGR_V26_SMOKE_PROJECTION`) | `E_8` residual (`s_{E_8}`, `ε_r`) is deliberately NOT implemented — the contract's own numbered build sequence (§VIII) does not list it as a phase, and CLM_KGR_018's control battery is unmet; router formula here is `s_base+γs_path+ηs_seion` only. All smoke runs are 1-epoch/small-subset — NOT a Gate 12 confirmatory campaign; MRR numbers produced are not meaningful benchmark results, only proof the pipeline executes correctly on real data |
| CLM_KGR_027 | DEFINITION | Reproducibility manifest set: `command.txt`, `environment.json`, `hardware.json`, `dataset_manifest.json`, `git_manifest.json`, `run_manifest.json`, `metrics.jsonl`, `final_metrics.json`, `rank_history.jsonl`, `error_attribution.jsonl`, `projection_audit.json`, `best.pt`/`last.pt` with `--resume`. | pattern reused from `papers/software_v4/main.tex`, `docs/governance.md` authority ladder | `seion_kgr/reproducibility.py` (checkpoint save/load + manifest functions, adapted from `seion_train_v25.py`); `train.py`'s epoch loop now calls `_module_diagnostics` + `rank_controller.compare_policies` -> `rank_history.jsonl`, `projection.measure_closure_leakage_sample` -> `error_attribution.jsonl`, `projection.audit_projectors` -> `projection_audit.json` (once, at completion), `repro.save_checkpoint`/`load_checkpoint` -> `last.pt` every epoch + `best.pt` on improvement. Verified end-to-end on real WN18RR data (`runs/KGR_V26_RESUME_TEST`): all files produced with real content (e.g. `projection_audit.json` isometry/idempotent residuals `~1e-7` after real training steps, confirming the CLM_KGR_029 fix holds under actual optimization, not just the unit test); a 1-epoch run followed by `--resume last.pt --epochs 2` correctly resumed at `start_epoch=1` with the carried-over `best_mrr` | No `kernel_manifest.json` (no fixed structural kernel exists in v26 — `E_8` is out of scope, see CLM_KGR_026). `rank_history.jsonl`'s `pathwise_score` and `gradient_sensitivity` fields are documented simplifications (last-batch grad norm as a proxy, closure-leakage reused as the pathwise-score proxy since true per-path gain propagation isn't implemented) — logged for diagnostic visibility only, this does NOT dynamically resize the model's rank at runtime |
| CLM_KGR_028 | DEFINITION | Query-conditioned path reasoner via budgeted BFS frontier expansion (fixed random top-k neighbor selector, not a learned A*Net priority network), with per-sample edge removal for the queried triple and its reciprocal during training. | contract §VII (message passing) + source note's selector requirement | `seion_kgr/reasoner.py::PathReasoner`, `Adjacency`; tests `test_edge_removal_excludes_the_queried_edge_from_the_frontier`, `test_reasoner_frontier_empty_when_head_has_no_outgoing_edges`, `test_states_for_candidates_uses_default_for_unreached_nodes` | Selector is a fixed uniform-random budget, explicitly not the learned A*Net priority function; multi-hop BFS is per-sample Python loop (correct but not batched/GPU-vectorized) — adequate for smoke-scale, not for a full FB15K-237 production campaign without further engineering |
| CLM_KGR_029 | PROVED_UNDER_ASSUMPTIONS | `StiefelProjector.Q()` (QR-retraction of a learned parameter) satisfies `Q^T Q = I` after gradient updates, not just at init. | standard QR-decomposition property (orthonormal `Q` by construction, any `R`) | `seion_kgr/kernels.py::StiefelProjector`; test `test_stiefel_projector_identities_hold_after_training_steps` — **caught a real bug**: an earlier version applied a QR sign-disambiguation via `torch.sign(diag(R)).clamp_min(1e-12)`, which replaced negative signs with `1e-12` and silently shrank that column to near-zero, breaking `Q^TQ=I` after training (residual `1.0` instead of `~0`); fixed by removing the unneeded sign correction | FP32 training-time projector, not the FP64 oracle's certified `Projector` — periodic audits should use `seion_kgr/projection.py::audit_projectors`, not assumed exact |

## Coverage note (updated after Fase 2-9)

Fase 0 (this matrix, the contract, the ledger) and Fase 1
(`seion_kgr_reference_fp64.py`) are complete — see the previous revision
of this note for their scope; both still pass (`python
seion_kgr_reference_fp64.py --self_test`).

**Fase 2** (`tests/kgr/`): 54 pytest tests across `test_algebra.py`,
`test_projection.py`, `test_evaluator_exactness.py`, `test_gradients.py`,
`test_negative_controls.py`, `test_seion_kgr_package.py`. Property-based
via `hypothesis` where practical. Run `python -m pytest tests/kgr -q`.
This suite caught two real bugs during this pass: a non-differentiable
cross-check path being asserted differentiable by mistake (test's
premise was wrong, fixed), and a genuine `StiefelProjector` sign-fix bug
that broke `Q^TQ=I` after training (see CLM_KGR_029).

**Fase 3-9** (`seion_kgr/` package, `seion_kgr_v26_train.py` entrypoint):
data/reproducibility/evaluate/scorers (Fase 3), `reasoner.py` (Fase 4),
`kernels.py` (Fase 5), `projection.py` (Fase 6), `rank_controller.py`
(Fase 7), `geometry.py` (Fase 8), `metaencoder.py` (Fase 9), wired
together in `model.py`/`train.py`. `python seion_kgr_v26_train.py
--self_test` runs all 4 base experts × path on/off × seion on/off (16
combinations) on a tiny synthetic graph — all pass, including a
positive/candidate score-path consistency check and a nonzero-gradient
check per combination.

**Real-data smoke runs executed** (not confirmatory — 1 epoch, small
eval subset, single seed, only meant to prove the pipeline runs
correctly end-to-end on real data): all 4 base experts on real WN18RR
data (`runs/KGR_V26_SMOKE_WN18RR_{complex,distmult,cp,tucker}`, `complex`
full valid/test 6-7s/epoch on CPU); path reasoner + seionic branch +
FI/associator losses jointly enabled on a real (truncated) WN18RR slice
(`runs/KGR_V26_SMOKE_PATH_SEION`, FI/assoc losses confirmed nonzero when
weight>0); projection layer (`path_proj_rank=6`) enabled jointly with the
above (`runs/KGR_V26_SMOKE_PROJECTION`). Each run directory has the
reproducibility manifest from CLM_KGR_027.

**Explicitly out of scope for this pass** (do not treat as done):
- `E_8` residual branch — not implemented, see CLM_KGR_026's limitation.
- Any Gate 12 confirmatory statistical campaign (multi-seed, multi-dataset,
  full epoch budgets) — the smoke runs above are existence proofs, not
  benchmark results, and must never be quoted as MRR/Hits@K findings.
- A learned A*Net-style priority selector for the path reasoner (fixed
  random budget only, CLM_KGR_028).
- The state→score→ranking certificate chain (CLM_KGR_020) remains `OPEN`
  — nothing in Fase 3-9 closes it; `margin_preserves_order` is still only
  checked in isolation.
