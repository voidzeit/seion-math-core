# Failed approaches

The initial specification asks for aggressive research breadth. This repository records a narrower executable nucleus first. A result is not promoted to a theorem merely because a numerical optimizer found a low residual. Continuous limits, physical interpretation, and application adapters remain separate tracks.

