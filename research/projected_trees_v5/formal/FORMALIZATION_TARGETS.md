# Formalization targets

The smallest useful formal kernel is:

1. orthogonal projection identity `P (I-P)=0`;
2. finite multilinear subset expansion;
3. projected/reduced root norm equivalence;
4. induction for the `(k-1)` projected bound;
5. scalar DAG forward/reverse identity;
6. coefficient-wise signed refinement.

Formalization is a future Lean/Coq/Isabelle track. No external proof assistant
claim is made by the executable Python tests.
