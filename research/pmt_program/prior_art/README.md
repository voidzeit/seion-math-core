# PRIOR-ART-R — reproducible prior-art review for Theorem R

Status: **first automated + agent-assisted pass complete; global label `NOVELTY_NOT_ESTABLISHED`**.
Summary of findings: `CLAIM_NOVELTY_MATRIX.md`.

| File / folder | Content |
|---|---|
| `SEARCH_PROTOCOL.md` | frozen protocol (claims R1a–R12, families A–N, sources, screening, stop criterion, deviations log) |
| `queries.json` | the 47 frozen queries |
| `FREEZE.json` | SHA-256 of protocol files before the first query + post hoc script fix hash |
| `harvest.py` | automated harvest (OpenAlex, arXiv, zbMATH Open, Semantic Scholar) → `raw/`, `SEARCH_LOG.csv`, `candidates.csv` |
| `anchors.json`, `snowball.py` | seed anchors and OpenAlex citation-graph snowballing (backward/forward) |
| `merge_pool.py` → `pool.csv` | deduplicated pool with rubric ordering tiers (ordering only, never exclusion) |
| `screening/` | Level-1 inputs/outputs (T1/T2 abstracts, T3 titles), consistency audit, Level-2 queue and results |
| `theorem_comparisons/` | Level-3 theorem-by-theorem comparisons (`L3_*.md`) |
| `l3_verdicts.json` | adjudicated verdicts + paper citation list (evidence pointers, disagreements recorded) |
| `build_matrix.py` → `PRIOR_ART_MATRIX.csv` | one row per screened source with final threat, level reached, verdict, citation flag |
| `build_bib.py` → `bibliography/references.bib` | Crossref-canonical BibTeX for cited sources (`NO_DOI` entries flagged for MR Lookup) |
| `MANUAL_QUERIES.md` | MathSciNet and Google Scholar steps the author must run and log |
| `quarantine/` | aborted attempt 1, mis-identified anchor outputs, duplicate comparison file (kept, not used) |

## Reproduce

```bash
python research/pmt_program/prior_art/harvest.py
```

```bash
python research/pmt_program/prior_art/snowball.py round1_seed
```

```bash
python research/pmt_program/prior_art/merge_pool.py
```

```bash
python research/pmt_program/prior_art/build_matrix.py
```

```bash
python research/pmt_program/prior_art/build_bib.py
```

API results drift over time; the raw responses of this pass are stored in `raw/`.
Level-1/2/3 judgments were produced by reading (agent-assisted) and are recorded per source; they are
not regenerated by the scripts.

## Claim numbering note

Canonical IDs are those of `SEARCH_PROTOCOL.md` §1. The tensor-network Level-3 files
(`L3_zhang2020_*`, `L3_baboulin2025_*`, `L3_grasedyck2010_*`, `L3_ceruti2023_*`) list the planar
extremizer as "R4/R5" jointly; `CLAIM_NOVELTY_MATRIX.md` normalizes this.

## 2026-09-14 — PRIOR-ART-R-HET (heterogeneous Theorem R, mini-round)

Status: **targeted round complete; global label `NOVELTY_NOT_ESTABLISHED`**. Claims H1 (heterogeneous bound), H2 (placement
independence), H4 (sharpness) and H3 (OPEN capped-equal-angle reduction). Summary: `CLAIM_NOVELTY_MATRIX_HET.md`. Round-1 files are
unchanged.

| File / folder | Content |
|---|---|
| `SEARCH_PROTOCOL_HET.md`, `queries_het.json`, `FREEZE_HET.json` | frozen protocol (families O1–O6, 30 queries), hashes before harvest, POST_HOC log |
| `het_round.py` | thin wrapper: runs round-1 `harvest.py`/`snowball.py`/`merge_pool.py`/`build_matrix.py`/`build_bib.py` unchanged, with redirected paths |
| `raw/het/`, `SEARCH_LOG_HET.csv`, `harvest_het_stdout.txt`, `retry_het_stdout.txt` | raw responses and request log |
| `candidates_het.csv`, `anchors_het.json` (verified), `snowball_candidates_het.csv`, `pool_het.csv` | harvest, 7 anchors, one-hop snowball, merged pool (`seen_in_round1`, `het_score`) |
| `screening/het_level1_build.py` → `screening/het_level1.csv`; `screening/het_level2.csv` | Level 1 (1178 rows) and Level 2 |
| `theorem_comparisons/L3_HET_*.md`, `l3_verdicts_het.json` | Level-3 comparisons (8 files) and adjudicated verdicts |
| `PRIOR_ART_MATRIX_HET.csv`, `bibliography/references_het.bib` | matrix (1188 rows) and bibliography (17 entries, 2 `NO_DOI`) |

```bash
python research/pmt_program/prior_art/het_round.py harvest
python research/pmt_program/prior_art/het_round.py retry
python research/pmt_program/prior_art/het_round.py snowball
python research/pmt_program/prior_art/het_round.py merge
python research/pmt_program/prior_art/screening/het_level1_build.py
python research/pmt_program/prior_art/het_round.py matrix
python research/pmt_program/prior_art/het_round.py bib
```

Manual actions pending: `CLAIM_NOVELTY_MATRIX_HET.md` §6 (Zniyed–Boyer PDF, MathSciNet/Scholar queries, arXiv/S2 re-run,
paywalled theorem numbering, tightness of Combettes–Yamada for m ≥ 3).

## 2026-09-14 — PRIOR-ART-R-CY (targeted follow-up: tightness of heterogeneous composition constants for m ≥ 3)

Status: **follow-up complete with coverage gaps; global label `NOVELTY_NOT_ESTABLISHED`**. Everything is in `followup_cy/`, and round-1 and HET files
are unchanged. Report: `followup_cy/CY_FOLLOWUP_REPORT.md`.

Answer to the priority question (does any work prove m ≥ 3 tightness of a heterogeneous composition constant, compute m ≥ 3 SRG disk/arc
products with a distance maximization, or imply g_Box?): (i) `NO_FOUND`; (ii) `PARTIAL`: the exact uncapped n-disc product is in
Chaffey–Forni–Sepulchre 2023, Thm 5 (threat 3), and exact membership criteria over products of phase-capped regions are in Yang–Chen–Qiu 2026,
preprint, Thm 5 (threat 3, distance maximization left open); (iii) `NO_FOUND`. No threat 4–5. Proposed (not applied) status notes for H1/H2/H3/H4
are in report §5.

| File / folder | Content |
|---|---|
| `followup_cy/PROTOCOL_CY.md`, `queries_cy.json`, `anchors_cy_spec.json`, `FREEZE_CY.json` | frozen protocol (22 queries, six-criterion rubric), hashes, POST_HOC log (§9) |
| `followup_cy/cy_round.py` (frozen), `cy_harvest_v2.py`, `cy_supplement.py`, `cy_verify_l3.py` (POST_HOC) | anchor verification, all-page forward citations, co-citation, harvest, Semantic Scholar citations, L3 DOI check |
| `followup_cy/raw/`, `SEARCH_LOG_CY.csv`, `*_stdout.txt` | raw responses and request log |
| `followup_cy/anchors_cy.json`, `forward_citations_cy.csv`, `cocitation_cy.csv`, `cocitation_cy_s2.csv`, `pool_cy.csv` | verified anchors, citations (OpenAlex union 186), co-citation sets, pool (605) |
| `followup_cy/screening_cy_L1_build.py` → `screening_cy_L1.csv` | Level 1 (672 rows incl. 67 Semantic-Scholar-only) |
| `followup_cy/L3_CY_*.md` (12), `l3_dois_cy.json` | Level-3 six-criteria sheets; Crossref DOI checks |
| `followup_cy/cy_numeric_check.py` → `cy_numeric_check_output.txt` | heuristic comparison of the Combettes–Yamada constant with g_Box (not a proof) |

```bash
cd research/pmt_program/prior_art/followup_cy
python cy_round.py verify && python cy_round.py forward && python cy_round.py cocite
python cy_harvest_v2.py            # POST_HOC scheduler; frozen alternative: python cy_round.py harvest
python cy_supplement.py oa_preprints && python cy_supplement.py s2_cites
python cy_round.py pool && python screening_cy_L1_build.py && python cy_verify_l3.py && python cy_numeric_check.py
```

Manual actions pending (report §7): the arXiv / Semantic Scholar keyword search was rate-limited throughout (0/22 each; re-run
`python cy_round.py retry`). Also pending: Google Scholar "cited by" pages (HRY20, RHY22, CY15, Chaffey 2023, Yang et al.), MathSciNet queries,
the Guthrie–Mallada PDF (bot check), Bünger–Rump J. Geom., and published numbering of the TAC/SIOPT papers.
