# P2 — k=2 sharpness status

## General class

The general fixed-eta projected constant remains open. The universal theorem
provides the normalized k=2 upper bound `C_T^P(eta) <= eta`, but no global
matching construction has been certified for every admissible law, topology,
dimension, rank, field, or repeated-map policy.

## Closed restricted class

For the homogeneous chain with the gated planar-rotation law:

```text
E_proj(eta) = eta^2
```

for the declared dimension/rank class. It reaches the universal k-1 upper
bound only at `eta=1`.

```text
general k=2 sharpness: OPEN
restricted gated-rotation class: PROVED
optimizer output: lower-bound/conjecture evidence only
```
