\section{Experiment configurations}

The primary configuration is \texttt{experiments/configs/finite\_ternary\_v1.yaml}. The canonical run matrix is \texttt{experiments/matrices/canonical\_run\_matrix.yaml}. Each row records dimensions, arity, field, representation, precision, backend, device, seed, sample count, tolerance, expected result, and failure interpretation.
