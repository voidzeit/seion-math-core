\section{Symbolic derivations}

The SymPy artifacts identify whether a result is a general symbolic derivation, a fixed-dimension calculation, or a counterexample. The curvature identity simplifies to zero as a formal expression. The projector artifact records $P^2-P=Q(Q^*Q-I)Q^*$, so idempotence requires the declared orthonormality assumption.

