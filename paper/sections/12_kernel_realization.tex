\section{Kernel-integrated realization}

On a finite measure grid, a kernel $K(p;q_1,\ldots,q_n)$ is applied by a weighted finite sum. This is an exact discrete model. Compact intervals, periodic grids, and finite graphs are represented by quadrature metadata. A Hilbert--Schmidt-style norm is recorded as a finite bound estimate; analytic boundedness and convergence are separate theorem tracks.

\input{generated/tables/computational_cost}
\begin{center}\includegraphics[width=.8\linewidth]{quadrature_convergence}\\\small Finite quadrature convergence diagnostic. The curve is generated from declared resolutions.\end{center}
