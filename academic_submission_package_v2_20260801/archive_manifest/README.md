# Archived pre-integration artifacts

These files were found untracked in the working tree before the
G3 branch merges and are confirmed superseded/duplicate, not deleted:

- `a_to_n_certification_v18_PRE_MERGE_BUILD_20260730.pdf`,
  `software_reproducibility_v5_PRE_MERGE_BUILD_20260730.pdf`,
  `supplementary_visual_atlas_v18_PRE_MERGE_BUILD_20260730.pdf` —
  older local pdflatex builds (2026-07-30 21:16) that predate the
  `main.tex` sources merged in from `research/spectral-a-to-n-v18`.
  The current, source-backed build is `papers/<name>/main.pdf`.
- `seion_master_audit_A_to_N_v17_blackwell_repro_fix_DUPLICATE_OF_spectral_legacy_v17.py` —
  byte-identical (mod CRLF/LF) to the now-tracked
  `spectral/legacy/v17/seion_master_audit_A_to_N_v17_blackwell_repro_fix.py`
  (verified with `diff -w`).

Original paths and sha256 hashes recorded in `ORIGINAL_HASHES.txt`.
