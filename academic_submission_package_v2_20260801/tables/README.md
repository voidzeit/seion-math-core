# tables/ — see per-paper directories

This package deliberately does not duplicate tables here — see
`sources/<paper>/` for each manuscript's own tables (kept alongside its
.tex source so provenance is unambiguous), and `ai_benchmarks/` /
`mathematical_certificates/` for the applied-benchmark and math-closure
tables respectively.
